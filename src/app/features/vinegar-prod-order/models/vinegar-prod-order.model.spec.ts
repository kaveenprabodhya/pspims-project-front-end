import { VinegarProdOrder } from './vinegar-prod-order.model';

describe('VinegarProdOrder', () => {
  it('should create an instance', () => {
    expect(new VinegarProdOrder()).toBeTruthy();
  });
});
