import { ProdOrderDetails } from './prod-order-details.model';

describe('ProdOrderDetails', () => {
  it('should create an instance', () => {
    expect(new ProdOrderDetails()).toBeTruthy();
  });
});
