import { BeverageIngredients } from './beverage-ingredients.model';

describe('BeverageIngredients', () => {
  it('should create an instance', () => {
    expect(new BeverageIngredients()).toBeTruthy();
  });
});
